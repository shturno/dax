/** @type {import('next').NextConfig} */
const nextConfig = {
  // Tell Next.js to use the app directory at root level
  // No specific configuration needed for this in newer Next.js versions
  // Just having the app directory at root should work
}

module.exports = nextConfig
